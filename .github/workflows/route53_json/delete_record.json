{
  "Changes": [
    {
      "Action": "DELETE",
      "ResourceRecordSet": {
        "Name": "${RECORD_NAME}",
        "Type": "${RECORD_TYPE}",
        "TTL": ${RECORD_TTL},
        "ResourceRecords": [
          {
            "Value": "${RECORD_VALUE}"
          }
        ]
      }
    }
  ]
}