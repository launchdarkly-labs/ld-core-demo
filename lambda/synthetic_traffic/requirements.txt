# Lambda runtime dependencies (excludes boto3/botocore which are
# provided by the Lambda Python 3.11 runtime).

# Template rendering
chevron>=0.14.0

# Utilities
python-dotenv>=1.0.0

# HTTP (for LD Management API calls in LDAPIUtils)
requests>=2.31.0

# LaunchDarkly
launchdarkly-server-sdk>=9.12.0
launchdarkly-server-sdk-ai>=0.16.0
launchdarkly-observability>=0.1.0

# OpenTelemetry
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
